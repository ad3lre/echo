/** Public inbox for support, legal, and safety (shown on auth UI; matches marketing). */
export const ECHO_PUBLIC_SUPPORT_EMAIL = 'support@app-echo.net';

export const echoPublicSupportMailtoHref = `mailto:${ECHO_PUBLIC_SUPPORT_EMAIL}`;
