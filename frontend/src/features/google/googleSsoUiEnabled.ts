/**
 * Google SSO entry points (login modal, welcome gate). Backend routes may still exist.
 * Set to `true` to show “Continue with Google” again.
 */
export const GOOGLE_SSO_SIGNIN_UI_ENABLED = false;
