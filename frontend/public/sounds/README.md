# Echo UI sounds (legacy / optional)

**The app bundles real clips from `src/assets/sounds/*.ogg`** (see `src/audio/echoSoundAssets.ts`). You do **not** need to duplicate files here.

This folder used to document optional MP3 drops; those paths are no longer used for playback.
