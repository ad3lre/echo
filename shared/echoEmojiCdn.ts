/** Cacheable same-origin GET for custom emoji bytes (cross-guild; no auth). */
export const ECHO_PUBLIC_EMOJI_CDN_PATH_PREFIX = '/api/v1/echo/public/emojis/';

/** S3/local object key prefix for published emoji CDN objects (Phase 2). */
export const ECHO_PUBLIC_EMOJI_OBJECT_KEY_PREFIX = 'echo/public-emojis/';
